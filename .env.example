# Copy to .env and fill in. Every value is optional — a missing key only
# disables the MCP server that needs it; the rest of BountyReaper still runs.

# --- Repo location -----------------------------------------------------------
# MCP servers are referenced relative to the repo root. Only set this if you
# launch your agent from outside the repo (otherwise leave it unset).
# BOUNTYREAPER_ROOT=/absolute/path/to/bounty-reaper

# --- Caido MCP ---------------------------------------------------------------
# Personal Access Token from Caido -> Settings -> Authentication.
# Requires Caido running locally.
CAIDO_URL=http://127.0.0.1:8080
CAIDO_PAT=

# --- Model provider ----------------------------------------------------------
# Only needed to run the standalone hackbrowser crawler outside BountyReaper.
# The integrated `hackbrowser` tool uses BountyReaper's configured provider.
# ANTHROPIC_API_KEY=
