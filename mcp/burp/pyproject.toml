[project]
name = "bountyreper-mcp-burp"
version = "0.1.0"
description = "Burp Suite MCP bridge - stdio server to the Burp MCP BApp SSE endpoint"
requires-python = ">=3.10"
dependencies = []

[tool.uv]
package = false
